{
  "name": "tests",
  "version": "1.0.0",
  "description": "Este diretório contém os testes automatizados para o Sistema de Controle de Farmácia, utilizando o framework Playwright para testes end-to-end.",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:produtos": "playwright test produtos.spec.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@playwright/test": "^1.55.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0"
  }
}
