# IMPORTANTE: Copie este arquivo para .env e preencha com valores reais
# NUNCA commite o arquivo .env no repositório

# Environment (development, staging, production)
NODE_ENV=development

# Database
DB_USER=admin
DB_PASSWORD=sua_senha_segura_aqui
DB_NAME=farmacia_dev

# JWT
JWT_SECRET=sua_chave_secreta_aqui_min_32_caracteres

# API
PORT=3001

# Domains - Development
FRONTEND_DOMAIN=farmacia-dev.diegolima.dev
API_DOMAIN=api-dev.diegolima.dev

# Domains - Production (example)
# NODE_ENV=production
# DB_NAME=farmacia_prod
# FRONTEND_DOMAIN=farmacia.diegolima.dev
# API_DOMAIN=api.diegolima.dev